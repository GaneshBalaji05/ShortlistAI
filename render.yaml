services:
  - type: web
    name: shortlistai
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn patched_app:app --host 0.0.0.0 --port $PORT
