services:
  - type: web
    name: shortlistai
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn export_runtime:app --host 0.0.0.0 --port $PORT
